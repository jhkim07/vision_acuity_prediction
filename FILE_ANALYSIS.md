# 파일 분석 리포트: 불완전하거나 불필요한 파일

## 🔴 삭제 권장 파일

### 1. 불완전한 파일

#### `step_7_va_measurement_v1.py`
- **문제**: import 문만 있고 실제 실행 코드가 없음 (17줄)
- **이유**: 같은 이름의 노트북 파일(`step_7_va_measurement_v1.ipynb`)이 실제 작업 파일
- **권장 조치**: 삭제 또는 내용 완성

### 2. 이전 버전 파일 (중복)

#### `crop.py`
- **문제**: `step_4_crop2.py`의 이전 버전
- **차이점**: 
  - `crop.py`: 고정된 크기로 중심 크롭 (818x818)
  - `step_4_crop2.py`: 검은 모서리를 자동 감지하여 크롭 (더 정교함)
- **권장 조치**: 삭제 (현재는 `step_4_crop2.py` 사용)

#### `kfold_dataset_split_old.py`
- **문제**: 이전 버전의 데이터 분할 스크립트
- **권장 조치**: 삭제 (현재는 `step_5_kfold_dataset_split_train_val_test_for_all_class_v2.py` 사용)

#### `kfold_dataset_split.py`
- **문제**: 이전 버전의 데이터 분할 스크립트
- **권장 조치**: 삭제

#### `kfold_dataset_split_train_val_test_for_all_class.py`
- **문제**: `step_5_kfold_dataset_split_train_val_test_for_all_class_v2.py`의 이전 버전
- **권장 조치**: 삭제

#### `kfold_dataset_split_train_val_test_for_each_class.py`
- **문제**: 이전 버전의 "각 클래스별" 분할 스크립트
- **권장 조치**: 삭제 (현재는 "all_class" 버전 사용)

#### `kfold_dataset_split_train_val_test_for_each_class_v2.py`
- **문제**: "각 클래스별" 분할 버전이지만 현재 프로젝트에서는 사용하지 않음
- **권장 조치**: 삭제 또는 `archive/` 폴더로 이동

### 3. 테스트/실험 파일 (프로젝트와 무관)

#### `test.py`
- **문제**: CIFAR-10 데이터셋을 사용한 간단한 CNN 테스트 코드
- **이유**: 실제 VA 예측 프로젝트와 무관
- **권장 조치**: 삭제 또는 `test_legacy/` 폴더로 이동

#### `rewards.pkl`
- **문제**: 프로젝트 코드에서 참조되지 않는 pickle 파일
- **권장 조치**: 삭제 (필요시 백업 후)

#### `cifar10_folds.json`
- **문제**: CIFAR-10 실험용 JSON 파일
- **이유**: `simple_test/` 폴더에서만 사용되며 메인 프로젝트와 무관
- **권장 조치**: 삭제 (또는 `simple_test/` 폴더 내에만 유지)

### 4. Dropbox 충돌 파일

#### `best_epoch_model (jin-Z690-UD-DDR4's conflicted copy 2025-02-15).pth`
- **문제**: Dropbox 동기화 충돌로 생성된 복사본 파일 (329MB)
- **이유**: 원본 파일(`best_epoch_model.pth`)이 이미 존재
- **권장 조치**: 삭제 (용량이 크므로 우선 삭제 권장)

## 🟡 검토 필요 파일/폴더

### `simple_test/` 폴더
- **내용**: 
  - CIFAR-10 실험용 스크립트들
  - EfficientNet 테스트 코드
  - 모델 평가 스크립트
- **상태**: 프로젝트 메인 워크플로우와는 분리된 실험 코드
- **권장 조치**: 
  - 실험 기록으로 유지하되, `experiments/` 또는 `archive/`로 이동 고려
  - 또는 README에 실험 폴더임을 명시

### `test_legacy/` 폴더
- **내용**: 레거시 데이터 분할 코드
- **권장 조치**: 
  - 더 이상 사용하지 않는다면 삭제
  - 참고용으로 유지하려면 `archive/` 폴더로 이동

### `output_datasets/` 폴더
- **상태**: `crop.py`의 출력 폴더로 사용됨 (현재 미사용)
- **권장 조치**: 확인 후 불필요하면 삭제

### `data_split/` 폴더
- **상태**: 이전 버전 스크립트들의 출력 폴더
- **권장 조치**: 현재는 `data_split_v2/` 사용 중이므로 확인 후 정리

## 📊 요약

### 삭제 권장 파일 목록 (즉시 삭제 가능)
```
step_7_va_measurement_v1.py  (불완전)
crop.py  (이전 버전)
kfold_dataset_split_old.py  (이전 버전)
kfold_dataset_split.py  (이전 버전)
kfold_dataset_split_train_val_test_for_all_class.py  (이전 버전)
kfold_dataset_split_train_val_test_for_each_class.py  (이전 버전)
kfold_dataset_split_train_val_test_for_each_class_v2.py  (미사용)
test.py  (무관한 테스트)
rewards.pkl  (참조 없음)
cifar10_folds.json  (CIFAR-10 실험용)
best_epoch_model (jin-Z690-UD-DDR4's conflicted copy 2025-02-15).pth  (충돌 복사본, 329MB)
```

### 검토 후 결정 파일/폴더
```
simple_test/  (실험 코드)
test_legacy/  (레거시 코드)
output_datasets/  (미사용 가능)
data_split/  (이전 버전 출력)
```

## ✅ 현재 프로젝트에서 사용 중인 파일 (유지 필요)

### 메인 워크플로우
1. `step_3_copy_patient_files.py`
2. `step_4_crop2.py`
3. `step_5_kfold_dataset_split_train_val_test_for_all_class_v2.py`
4. `step_6_compuate_mean_std.py`
5. `step_7_va_measurement_v1.ipynb`
6. `step_11_convert_label_4_classes.py`

### 기타 중요 파일
- `README.md`
- `researchnote.ipynb` (연구 노트)
- 학습된 모델 파일들 (`.pth` 파일들, 충돌 복사본 제외)

## 💾 디스크 공간 절약

충돌 복사본 파일(`best_epoch_model (jin-Z690-UD-DDR4's conflicted copy 2025-02-15).pth`)만 삭제해도 **329MB** 절약 가능합니다.

